import * as React from 'react';
import { ProfileCard } from './ProfileCard';

export default {
  title: 'ProfileCard',
  component: ProfileCard,
};

export const regular = (): React.ReactNode => <ProfileCard />;
