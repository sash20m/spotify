import * as React from 'react';
import { VolumeController } from './VolumeController';

export default {
  title: 'VolumeController',
  component: VolumeController,
};

export const regular = (): React.ReactNode => <VolumeController />;
