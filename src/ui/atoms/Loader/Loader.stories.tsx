import * as React from 'react';
import { LoaderInline } from './Loader';

export default {
  title: 'LoaderInline',
  component: LoaderInline,
};

export const regular = (): React.ReactNode => <LoaderInline />;
